import PlaceholderPage from "@/components/PlaceholderPage";

export default function LeaderboardPage() {
  return (
    <PlaceholderPage
      eyebrow="Phase 4c"
      title="Leaderboard"
      description="Ranked artifacts by usage, royalties, and compute jobs are coming soon. The metric plumbing exists; the view ships in a later phase."
    />
  );
}
